name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - openmm

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
