name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - openmm>=7.6
  - numpy<1.24
  - pyyaml
  - importlib_resources

    # Testing
  - scipy
  - openmmtools
  - mdtraj
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
