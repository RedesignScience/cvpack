name: test
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - openmm
  - numpy<1.24
  - pyyaml

    # Testing
  - scipy
  - openmmtools
  - mdtraj
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov
